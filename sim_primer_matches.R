install.packages('dplyr')

library(dplyr)
source('clean_data.R')

#get score of match

get_score <- function(home, away){
  #try to get from history, pair
  subset <- his_pair.pl[which(hist_pair.pl$HomeTeam == home & hist_pair]dplyr
}